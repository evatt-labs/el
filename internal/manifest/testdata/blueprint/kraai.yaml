version: 1

providers:
  compute:
    vendor: cloudflare
  postgres:
    vendor: neon
    settings:
      project: kraai-control-plane
      database: appdb
      role: app

hooks: ./kraai.hooks.mjs

plugins:
  - kraai-plugin-example.wasm
  - ./plugins/cost-guard.wasm
