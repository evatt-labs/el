node_modules/
coverage/
.env
.env.*
!.env.example
.kraai-deploy-*.json
.DS_Store

# Go rewrite
/dist/
*.test
/coverage.out

# npm/ postinstall-downloaded binary (see npm/scripts/postinstall.js)
npm/.bin/

# Agent worktrees. Created inside the repo by tooling, and each is its own
# git checkout — committing one adds a gitlink to a directory nobody else has.
.claude/worktrees/
